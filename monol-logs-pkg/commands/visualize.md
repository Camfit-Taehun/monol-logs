---
description: 세션 시각화 - 타임라인, 대시보드, 통계 (한글: 시각화, 대시보드, 타임라인)
argument-hint: "[--ascii|--md|--html] [--author <name>] [--date <range>]"
allowed-tools: [Read, Write, Bash, Glob, Grep]
use_when:
  - 사용자가 "시각화", "대시보드", "타임라인" 등을 언급할 때
  - 세션 통계를 보고 싶을 때
  - 팀 활동을 시각적으로 확인하고 싶을 때
  - 세션 히스토리를 한눈에 보고 싶을 때
---

# /visualize - 세션 시각화

세션 활동을 시각화하여 타임라인, 통계, 대시보드를 생성합니다.

## 사용법

```
/visualize                      # 기본: 터미널 ASCII 타임라인
/visualize --ascii              # 터미널 ASCII 타임라인
/visualize --md                 # 마크다운 파일 생성
/visualize --html               # 인터랙티브 HTML 대시보드

# 필터링 옵션
/visualize --author <name>      # 특정 작성자만
/visualize --date 7d            # 최근 7일
/visualize --date 2026-01-01:2026-01-19  # 날짜 범위
/visualize --topic <search>     # 토픽 검색

# 출력 옵션
/visualize --output <path>      # 커스텀 출력 경로 (--md, --html)
/visualize --open               # 생성 후 자동 열기 (--html)
```

## 인자: $ARGUMENTS

## 동작

### 1. 인자 파싱

- `--ascii`: 터미널 ASCII 출력 (기본값)
- `--md`: 마크다운 파일 생성
- `--html`: HTML 대시보드 생성
- `--author <name>`: 작성자 필터
- `--date <range>`: 날짜 필터
  - `7d`, `30d`: 최근 N일
  - `YYYY-MM-DD:YYYY-MM-DD`: 날짜 범위
- `--topic <search>`: 토픽 검색
- `--output <path>`: 출력 경로 지정
- `--open`: 생성 후 자동 열기

### 2. 세션 데이터 수집

`.claude/sessions/*.meta.json` 파일들을 스캔:

```bash
for meta_file in .claude/sessions/*.meta.json; do
  # jq로 파싱
  sessionId=$(jq -r '.sessionId' "$meta_file")
  topic=$(jq -r '.topic' "$meta_file")
  createdAt=$(jq -r '.createdAt' "$meta_file")
  savedAt=$(jq -r '.savedAt' "$meta_file")
  savedBy=$(jq -r '.savedBy' "$meta_file")
  messageCount=$(jq -r '.messageCount' "$meta_file")
done
```

### 3. 필터 적용

수집된 세션에 필터 적용:
- `--author`: savedBy 매칭
- `--date`: createdAt 범위 필터
- `--topic`: topic 부분 문자열 매칭

### 4. ASCII 출력 (--ascii / 기본)

#### 4.1 타임라인 뷰

```
Session Timeline - {project-name}
Generated: {timestamp}

Legend: @ alice  # bob  * carol
════════════════════════════════════════════════════════════════

{date} ({relative})
├─ {start-time} ──────────────────────────────────── {end-time}
│  {symbol} {bar} {topic} ({msgs} msgs, {id})
│
└─ {start-time} ──────────────────────────────────── {end-time}
   {symbol} {bar} {topic} ({msgs} msgs, {id})

════════════════════════════════════════════════════════════════
Summary: {n} sessions | {n} authors | {n} total messages
```

#### 4.2 작성자별 색상/심볼 할당

```bash
# 해시 기반 결정적 할당
SYMBOLS=("@" "#" "*" "+" "=" "&")
COLORS=("\033[31m" "\033[32m" "\033[33m" "\033[34m" "\033[35m" "\033[36m")

get_author_style() {
  local hash=$(echo "$author" | md5 | cut -c1-8)
  local idx=$((16#$hash % 6))
  echo "${SYMBOLS[$idx]} ${COLORS[$idx]}"
}
```

#### 4.3 막대 길이 계산

- 시간 범위를 터미널 너비에 맞춤
- 메시지 수에 따라 막대 두께 조절

### 5. 마크다운 출력 (--md)

파일 생성: `.claude/sessions/visualization.md` (또는 --output 경로)

```markdown
# Session Visualization

Generated: {timestamp}
Total Sessions: {n} | Authors: {n} | Messages: {n}

## Timeline (Last {n} Days)

```
    {date1}   {date2}   {date3}
alice ═══════         ════
bob       ═══════════
carol          ═══         ═══
```

## Sessions by Date

### {date}

| Time | Author | Topic | Duration | Msgs | Links |
|------|--------|-------|----------|------|-------|
| {time} | {author} | {topic} | {duration} | {msgs} | [summary](...) |

## Author Statistics

```
Author      Sessions    Messages    Avg Duration
─────────────────────────────────────────────────
alice          12          520         2h 15m
bob             8          312         1h 45m
─────────────────────────────────────────────────
Total          20          832         2h 00m
```

---
*Generated by monol-logs /visualize*
```

### 6. HTML 대시보드 출력 (--html)

파일 생성: `.claude/sessions/dashboard.html` (또는 --output 경로)

#### 6.1 템플릿 구조

단일 HTML 파일에 CSS와 JavaScript 내장:

```html
<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8">
  <title>Session Dashboard - {project}</title>
  <style>/* 내장 CSS */</style>
</head>
<body>
  <div id="app">
    <header><!-- 필터 UI --></header>
    <main>
      <section id="timeline"><!-- 타임라인 --></section>
      <section id="sessions"><!-- 세션 카드 --></section>
      <section id="stats"><!-- 통계 --></section>
    </main>
  </div>
  <script>
    const SESSION_DATA = /* 세션 데이터 JSON */;
    // 대시보드 로직
  </script>
</body>
</html>
```

#### 6.2 기능

- **필터링 UI**: 작성자, 날짜 범위, 토픽 검색
- **인터랙티브 타임라인**: 가로 막대, 호버 상세정보
- **세션 카드 그리드**: 반응형 레이아웃
- **통계 요약**: 세션 수, 작성자 수, 총 메시지 수
- **다크/라이트 테마**: 토글 버튼
- **클립보드 복사**: `/session view <id>` 명령어 복사

#### 6.3 자동 열기

`--open` 옵션 시:
```bash
# macOS
open ".claude/sessions/dashboard.html"
# Linux
xdg-open ".claude/sessions/dashboard.html"
```

### 7. 결과 출력

#### ASCII

터미널에 직접 출력

#### 마크다운

```
Visualization saved:
  📄 .claude/sessions/visualization.md

💡 팁:
  - 마크다운 뷰어에서 열어보세요
  - VS Code, Obsidian 등에서 미리보기 가능
```

#### HTML

```
Dashboard generated:
  🌐 .claude/sessions/dashboard.html

💡 팁:
  - 브라우저에서 열어 인터랙티브 대시보드 확인
  - /visualize --html --open 으로 자동 열기
```

## 예시

```
/visualize
→ 터미널에 ASCII 타임라인 출력

/visualize --md
→ visualization.md 파일 생성

/visualize --html --open
→ dashboard.html 생성 후 브라우저에서 열기

/visualize --author alice --date 7d
→ alice의 최근 7일 세션만 시각화

/visualize --html --output ./docs/team-sessions.html
→ 커스텀 경로에 HTML 저장
```

## 관련 커맨드

- `/sessions` - 세션 목록 조회
- `/session view` - 세션 내용 보기
- `/save` - 세션 저장
